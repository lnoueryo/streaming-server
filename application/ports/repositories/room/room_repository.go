package room_repository

import (
	// "streaming-server.com/domain/entities/room"
)

type Interface interface {
    // GetRoomByID(id int) (*room_entity.Room, error)
    // CreateRoom(id int) (*room_entity.Room, error)
    // DeleteRoom(id int) error
}